name: Backend CI/CD

on:
  workflow_dispatch:
    inputs:
      aws-region:
        description: "AWS Region"
        required: false
        type: string
        default: "eu-north-1"

      image-tag:
        description: "Docker image tag (e.g. 1.0.0)"
        required: true
        type: string

permissions:
  id-token: write
  contents: read

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      # Checkout
      - name: Checkout Repository
        uses: actions/checkout@v6

      # Setup Node
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      # Install dependencies
      - name: Install Auth Service Dependencies
        run: cd apps/backend/auth-service && npm install

      # Run tests
      - name: Run Auth Service Tests
        run: cd apps/backend/auth-service && npm test

      # Configure AWS credentials (OIDC)
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ inputs.aws-region }}
          role-session-name: GitHubActions-${{ github.run_id }}

      # Login to ECR
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # Build Docker image
      - name: Build Auth Service Image
        run: |
          docker build -t auth-service:${{ inputs.image-tag }} ./apps/backend/auth-service

      # Tag image for ECR
      - name: Tag for ECR
        run: |
          docker tag auth-service:${{ inputs.image-tag }} \
          ${{ steps.login-ecr.outputs.registry }}/auth-service:${{ inputs.image-tag }}

      # Push image
      - name: Push to ECR
        run: |
          docker push ${{ steps.login-ecr.outputs.registry }}/auth-service:${{ inputs.image-tag }}

